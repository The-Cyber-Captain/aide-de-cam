name: README sync check

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sync README
        run: ./scripts/sync_readme.sh
      - name: Fail if changes required
        run: |
          if ! git diff --quiet; then
            echo "README copies are out of date. Run ./scripts/sync_readme.sh and commit the result."
            git diff
            exit 1
          fi
