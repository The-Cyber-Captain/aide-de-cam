cd path\to\Aide-De-Cam\plugin

# First time only - generate Gradle wrapper
gradle wrapper --gradle-version 8.2

# Build the plugin
gradlew.bat assembleRelease

The built AAR will output at:
plugin/build/outputs/aar/plugin-release.aar

# Copy into:
addons/aide_de_cam/android/

# Follow installation as per a pre-compiled Release from here on out.
(Basically, copy "addons" into your Godot project and enable. See: main README.md)